{
  "name": "remix-microfrontend-demo",
  "version": "1.0.0",
  "description": "Vite + Module Federation is now possible",
  "main": "index.js",
  "scripts": {
    "install:deps": "pnpm install && pnpm -r install",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r start"
  },
  "license": "ISC",
  "devDependencies": {
  }
}
